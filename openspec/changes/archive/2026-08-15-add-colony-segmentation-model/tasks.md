# Tasks: add-colony-segmentation-model

## 1. Подготовка импортёра COCO-bbox → контракт данных

- [x] 1.1 Добавить `CocoBboxImporter(BaseAdapter)` в `train/dataset_adapters.py`: чтение `annot_COCO.json`, разбор списков `images` (путь, ширина, высота) и `annotations` (bbox, `image_id`) из `datasets/22022540`.
- [x] 1.2 Реализовать растризацию бокса во **вписанный эллипс** (центр = `(x+w/2, y+h/2)`, радиусы = `w/2, h/2`) через `cv2.ellipse` в бинарную маску 255/0.
- [x] 1.3 Пропускать изображения без боксов (маска не создаётся, запись в манифест не попадает).
- [x] 1.4 Автопоиск геометрии чашки (`dish={cx,cy,r}`) для `kind=source` через переиспользование `analysis/colony_detector.py` (выполняется в режиме `--crop`; для source-only-импорта пропускается ради скорости).
- [x] 1.5 Опциональный режим `kind=cropped`: обрезка по кругу чашки с чёрным фоном вне `r` для изображения и маски (флаг `--crop`).
- [x] 1.6 Реализовать материализацию в самодостаточную папку `storage="copy"`: копии файлов + `dataset.json` со схемой `training-data-contract` (`mask_mode="binary"`, `origin="external"`, `dish`).
- [x] 1.7 Верификация корректности: сверка числа созданных масок на снимок с числом боксов из COCO **и** из YOLO/VOC-дублей; предупреждение при расхождении > порога.

## 2. CLI и интеграция в проект

- [x] 2.1 Добавить CLI-вход `import-22022540` (`uv run import-22022540 --data-root ... --output ... [--crop]`), печать сводки (обработано, source/cropped записей, путь к `dataset.json`).
- [x] 2.2 Убедиться, что `load_manifest(<output>)` резолвится через `ManifestAdapter` и содержит все записи; покрыть простой проверкой (ручн./pytest).
- [x] 2.3 Документировать импортёр/CLI в `AGENTS.md` (раздел про внешние импортёры).

## 3. Обучение бинарной сегментации

- [x] 3.1 Установить/использовать `.venv-full` (torch, onnxruntime, albumentations) и прогнать `import-22022540` для получения датасета.
- [x] 3.2 Запустить `uv run python -m train.main train --model unet --data-root <output> --img-size 512 --epochs <N>` с seed-сплитом; зафиксировать baseline метрики.
- [x] 3.3 Оценить качество на плотных чашках (2–747 колоний); при `Dice < 0.6` — скорректировать растризацию/стратификацию и дообучить.
- [x] 3.4 Получить `best.onnx` из `train/runs/<run>/`.

## 4. Интеграция модели как алгоритма

- [x] 4.1 Поместить `best.onnx` в `models/` (bundled), убедиться, что `list_algorithms()` регистрирует `OnnxModelAlgorithm` при старте.
- [x] 4.2 Проверить `detect(image)->uint8 mask` (порог 0.5) на тестовых снимках; согласовать выход с размером входа.
- [x] 4.3 (Запасной вариант) Проверить путь внешнего ONNX через GUI/CLI без пересборки.
- [x] 4.4 Документировать модель и шаги воспроизведения обучения в `AGENTS.md`.